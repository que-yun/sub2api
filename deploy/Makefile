# 本目录没有独立构建入口。
# 请到仓库根目录使用：
#   make build
#   make run
#   make deploy

.PHONY: help
help:
	@echo "请到仓库根目录使用唯一入口："
	@echo "  make build"
	@echo "  make run"
	@echo "  make deploy"
	@exit 1

.DEFAULT_GOAL := help
